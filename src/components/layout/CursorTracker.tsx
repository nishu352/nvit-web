"use client";

/**
 * CursorTracker disabled to eliminate window mousemove listeners,
 * requestAnimationFrame loops, and GPU/CPU rendering overhead.
 */
export default function CursorTracker() {
  return null;
}
